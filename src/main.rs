mod tuples;
mod canvas;
mod projectile;
mod matrix;
mod clock;

fn main() {
    // tuples::tuple_tests();
    // canvas::canvas_tests();
    // projectile::projectile_plot();
    // matrix::matrix_tests();
    // matrix::transform_tests();
    clock::clock_render();
}
